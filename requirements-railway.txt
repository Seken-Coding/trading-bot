# Railway Optimized Requirements
# Minimal dependencies for cloud deployment

# Core trading (required)
alpaca-trade-api==3.0.2
pandas==2.0.3
numpy==1.24.4

# HTTP and async (lightweight)
aiohttp==3.8.5
requests==2.31.0

# Database (Railway PostgreSQL)
psycopg2-binary==2.9.7

# Web framework (minimal)
flask==2.3.3
flask-cors==4.0.0

# Plotting (lightweight alternative)
plotly==5.16.1

# Environment and utilities
python-dotenv==1.0.0
pytz==2023.3

# Logging and monitoring
structlog==23.1.0
